# Docker Copilot 前端

一个精美的Docker容器管理平台前端界面，支持浅色/深色主题切换。

## 功能特性

- 🎨 **精美UI设计** - 现代化的界面设计，优秀的用户体验
- 🌙 **主题切换** - 支持浅色、深色和跟随系统主题
- 📱 **响应式设计** - 完美适配桌面和移动设备
- ⚡ **实时交互** - 流畅的容器操作体验
- 🔐 **安全认证** - JWT令牌认证机制

## 技术栈

- **前端框架**: React 18
- **构建工具**: Vite
- **样式**: Tailwind CSS
- **图标**: Lucide React
- **HTTP客户端**: Axios
- **状态管理**: React Hooks

## 快速开始

### 环境要求

- Node.js 16+ 
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 项目结构

```
src/
├── components/          # React组件
│   ├── Auth.jsx        # 认证组件
│   ├── Header.jsx      # 导航头部
│   ├── Containers.jsx   # 容器管理
│   ├── Images.jsx      # 镜像管理
│   └── Settings.jsx    # 系统设置
├── hooks/               # 自定义Hook
│   └── useTheme.js     # 主题管理
├── api/                 # API相关
│   └── client.js       # API客户端
├── utils/               # 工具函数
│   └── cn.js           # CSS类名合并
├── App.jsx             # 主应用组件
├── main.jsx            # 应用入口
└── index.css           # 全局样式
```

## API接口

前端与后端API完全对接，支持以下功能：

### 认证相关
- `POST /api/auth` - 用户认证

### 版本管理
- `GET /api/version` - 获取版本信息
- `PUT /api/program` - 更新程序

### 容器管理
- `GET /api/containers` - 获取容器列表
- `POST /api/container/{id}/start` - 启动容器
- `POST /api/container/{id}/stop` - 停止容器
- `POST /api/container/{id}/restart` - 重启容器
- `POST /api/container/{id}/rename` - 重命名容器
- `POST /api/container/{id}/update` - 更新容器
- `GET /api/container/backup` - 容器备份
- `GET /api/container/listBackups` - 备份列表
- `POST /api/container/backups/{filename}/restore` - 恢复容器
- `DELETE /api/container/backups/{filename}` - 删除备份
- `GET /api/container/backup2compose` - 备份为Compose

### 镜像管理
- `GET /api/images` - 获取镜像列表
- `DELETE /api/image/{id}` - 删除镜像

### 进度查询
- `GET /api/progress/{taskid}` - 查询任务进度

## 主题系统

项目内置完整的主题切换功能：

- **浅色模式** - 明亮舒适的界面
- **深色模式** - 护眼的暗色界面  
- **跟随系统** - 自动匹配系统主题设置

主题设置会自动保存到本地存储，下次访问时保持用户偏好。

## 部署说明

### 开发环境

确保后端服务运行在 `http://192.168.50.4:12712`

### 生产环境

1. 构建项目：`npm run build`
2. 部署 `dist` 目录到Web服务器
3. 配置后端API地址（如需修改，请更新 `src/api/client.js`）

## 浏览器支持

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## 许可证

MIT License